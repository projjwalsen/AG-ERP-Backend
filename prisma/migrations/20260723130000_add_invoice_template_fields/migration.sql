-- Invoice template rendering is file based; this migration intentionally
-- has no database schema changes.  The directory must nevertheless contain
-- a migration file for Prisma's ordered deployment history.
SELECT 1;
